<?php
/**
 * underscore.js template for the dialog for the cred_user_form shortcode.
 *
 * @since m2m
 */
?>
<script type="text/html" id="tmpl-cred-user-edit-form-template">
	<#
	data = _.extend(
		data,
		{
			attributes: {
				userSelection: {
					header: '<?php echo esc_js( __( 'User selection', 'wp-cred' ) ); ?>',
					fields: {
						user: {
							label: '<?php echo esc_js( __( 'User to edit', 'wp-cred' ) ); ?>',
							type: 'user',
							defaultValue: 'current'
						}
					}
				}
			}
		}
	);
	#>
	<# print( data.templates.dialog( data ) ); #>
</script>
